{
  "name": "edusmart-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend-flask"
  ],
  "scripts": {
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend-flask && python app.py"
  }
}